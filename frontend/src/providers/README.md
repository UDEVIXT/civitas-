## /providers
Carpeta dedicada a los Context Providers de React (o wrappers similares) que envuelven la aplicación para inyectar datos o estados.

### Ejemplos
AuthProvider, ThemeProvider, NotificationProvider.

Generalmente se importan en el App.tsx o main.tsx para proveer acceso global a estados críticos.